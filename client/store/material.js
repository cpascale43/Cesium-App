// Edit this file to use Redux.

/*

import axios from 'axios'

// Action Types
const SET_MATERIALS = 'SET_MATERIALS'


// Initial State
const materials = []


// Action Creators
const setMaterials = materials => ({type: SET_MATERIALS, materials})


// Thunk Creators
export const fetchMaterials = () => {
  return async dispatch => {
    try {
      const { data } = await axios.get(URL_HERE);
      dispatch(setMaterials(data));
    } catch (error) {
      console.error(error);
    }
  };
};


// Reducer
export default function(state = materials, action) {
  switch (action.type) {
    case SET_MATERIALS:
      return action.materials
    default:
      return state
  }
}

*/
